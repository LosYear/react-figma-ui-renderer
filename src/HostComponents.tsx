// @ts-nocheck
import * as React from 'react';

const Frame = (props) => {
    return <frame {...props}/>;
};

const Rectangle = (props) => {
    return <rectangle {...props}/>;
};

export {Frame, Rectangle};